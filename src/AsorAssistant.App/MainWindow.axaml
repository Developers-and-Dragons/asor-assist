<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:AsorAssistant.App.ViewModels"
        xmlns:views="using:AsorAssistant.App.Views"
        x:Class="AsorAssistant.App.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        x:Name="MainWin"
        Title="ASOR Assistant"
        Width="1200" Height="850"
        MinWidth="900" MinHeight="650">

  <Grid ColumnDefinitions="72,*">

    <!-- Navigation Rail -->
    <Border Grid.Column="0" Background="{StaticResource AppSurfaceBrush}"
            BorderBrush="{StaticResource AppBorderSubtleBrush}" BorderThickness="0,0,1,0">
      <DockPanel>
        <!-- Brand -->
        <StackPanel DockPanel.Dock="Top" Margin="0,16,0,16" HorizontalAlignment="Center">
          <TextBlock Text="ASOR" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center"
                     Foreground="{StaticResource AppAccentBrush}" />
          <TextBlock Text="Assistant" FontSize="8" HorizontalAlignment="Center"
                     Foreground="{StaticResource AppTextMutedBrush}" Margin="0,1,0,0" />
        </StackPanel>

        <!-- Help at bottom -->
        <StackPanel DockPanel.Dock="Bottom" Margin="0,0,0,12">
          <Separator Margin="10,0" Background="{StaticResource AppBorderSubtleBrush}" />
          <Button HorizontalAlignment="Center" Margin="0,10,0,0"
                  Background="Transparent" CornerRadius="8" Padding="8"
                  ToolTip.Tip="Help &amp; Documentation">
            <Button.Flyout>
              <Flyout Placement="RightEdgeAlignedTop">
                <StackPanel Spacing="8" Width="280" Margin="4">
                  <TextBlock Text="ASOR Assistant" FontWeight="SemiBold" FontSize="14" />
                  <TextBlock TextWrapping="Wrap" FontSize="12"
                             Foreground="{StaticResource AppTextSecondaryBrush}"
                             Text="Register external agents against the Workday Agent System of Record (ASOR) API v1.2." />
                  <TextBlock Text="→ ASOR v1.2 Specification" FontSize="12"
                             Foreground="{StaticResource AppLinkBrush}" Cursor="Hand" />
                  <Separator Background="{StaticResource AppBorderSubtleBrush}" />
                  <TextBlock TextWrapping="Wrap" FontSize="11"
                             Foreground="{StaticResource AppTextMutedBrush}"
                             Text="Regions: US · EU · UK · SIN · IND · JPN&#x0a;Endpoints: {region}.agent.workday.com" />
                </StackPanel>
              </Flyout>
            </Button.Flyout>
            <StackPanel Spacing="3" HorizontalAlignment="Center">
              <TextBlock Text="?" FontSize="16" HorizontalAlignment="Center" FontWeight="Bold"
                         Foreground="{StaticResource AppTextMutedBrush}" />
              <TextBlock Text="Help" FontSize="9" HorizontalAlignment="Center"
                         Foreground="{StaticResource AppTextMutedBrush}" />
            </StackPanel>
          </Button>
        </StackPanel>

        <!-- Nav items -->
        <ListBox Classes="nav-rail" SelectedIndex="{Binding SelectedPageIndex}">
          <ListBoxItem ToolTip.Tip="Agent Definition Editor">
            <StackPanel Spacing="3" HorizontalAlignment="Center">
              <TextBlock Text="✏" FontSize="18" HorizontalAlignment="Center" />
              <TextBlock Text="Editor" FontSize="9" HorizontalAlignment="Center"
                         Foreground="{StaticResource AppTextSecondaryBrush}" />
            </StackPanel>
          </ListBoxItem>
          <ListBoxItem ToolTip.Tip="JSON Preview">
            <StackPanel Spacing="3" HorizontalAlignment="Center">
              <TextBlock Text="{}{}" FontSize="15" HorizontalAlignment="Center" FontFamily="Cascadia Mono, Consolas, monospace" />
              <TextBlock Text="JSON" FontSize="9" HorizontalAlignment="Center"
                         Foreground="{StaticResource AppTextSecondaryBrush}" />
            </StackPanel>
          </ListBoxItem>
          <ListBoxItem ToolTip.Tip="Saved Drafts">
            <StackPanel Spacing="3" HorizontalAlignment="Center">
              <TextBlock Text="📄" FontSize="18" HorizontalAlignment="Center" />
              <TextBlock Text="Drafts" FontSize="9" HorizontalAlignment="Center"
                         Foreground="{StaticResource AppTextSecondaryBrush}" />
            </StackPanel>
          </ListBoxItem>
          <ListBoxItem ToolTip.Tip="Service Operation Lookups">
            <StackPanel Spacing="3" HorizontalAlignment="Center">
              <TextBlock Text="🔍" FontSize="18" HorizontalAlignment="Center" />
              <TextBlock Text="Lookups" FontSize="9" HorizontalAlignment="Center"
                         Foreground="{StaticResource AppTextSecondaryBrush}" />
            </StackPanel>
          </ListBoxItem>
        </ListBox>
      </DockPanel>
    </Border>

    <!-- Main Content -->
    <DockPanel Grid.Column="1">

      <!-- Connection Bar: always visible, prominent -->
      <Border DockPanel.Dock="Top" Classes="connection-bar">
        <StackPanel Spacing="6">
          <!-- Summary row with inline toggle -->
          <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="Connection" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"
                       Foreground="{StaticResource AppTextPrimaryBrush}" />

            <Border Classes="badge-success" IsVisible="{Binding HasToken}">
              <TextBlock Text="✓ Token configured" FontSize="10" Foreground="{StaticResource AppSuccessBrush}" FontWeight="Medium" />
            </Border>
            <Border Classes="badge-warning" IsVisible="{Binding !HasToken}">
              <TextBlock Text="⚠ Bearer token required" FontSize="10" Foreground="{StaticResource AppWarningBrush}" FontWeight="Medium" />
            </Border>

            <Button Classes="action-subtle" Command="{Binding ToggleConnectionBarCommand}"
                    ToolTip.Tip="Show/hide token input" VerticalAlignment="Center"
                    IsVisible="{Binding !IsConnectionBarExpanded}">
              <TextBlock Text="▾" FontSize="12" />
            </Button>
            <Button Classes="action-subtle" Command="{Binding ToggleConnectionBarCommand}"
                    ToolTip.Tip="Show/hide token input" VerticalAlignment="Center"
                    IsVisible="{Binding IsConnectionBarExpanded}">
              <TextBlock Text="▴" FontSize="12" />
            </Button>
          </StackPanel>

          <!-- Expanded: token input -->
          <StackPanel IsVisible="{Binding IsConnectionBarExpanded}" Spacing="4">
            <TextBlock Classes="field-label" Text="BEARER TOKEN" />
            <TextBox Text="{Binding BearerToken}" PlaceholderText="Paste your bearer token from the Workday Developer Site"
                     PasswordChar="*" RevealPassword="False" MaxWidth="600" HorizontalAlignment="Left" />
            <TextBlock Classes="helper"
                       Text="Generate a token at the Workday Developer Site (API Client → Generate Token). Session-only — not saved to disk." />
          </StackPanel>
        </StackPanel>
      </Border>

      <!-- Page Content -->
      <Panel>
        <!-- Editor + Registration Panel -->
        <Grid IsVisible="{Binding IsEditorPage}">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <views:DefinitionEditorView DataContext="{Binding Editor}" />
          <Border Grid.Column="1" Classes="panel" Width="400"
                  IsVisible="{Binding IsRegistrationPanelOpen}">
            <views:RegistrationView DataContext="{Binding Registration}" />
          </Border>
        </Grid>

        <views:JsonPreviewView DataContext="{Binding JsonPreview}"
                               IsVisible="{Binding #MainWin.((vm:MainWindowViewModel)DataContext).IsJsonPage}" />
        <views:DraftManagerView DataContext="{Binding DraftManager}"
                                IsVisible="{Binding #MainWin.((vm:MainWindowViewModel)DataContext).IsDraftsPage}" />
        <views:WqlLookupView DataContext="{Binding WqlLookup}"
                             IsVisible="{Binding #MainWin.((vm:MainWindowViewModel)DataContext).IsLookupsPage}" />
      </Panel>
    </DockPanel>
  </Grid>

</Window>
